# Backend API base URL (no trailing slash).
# If unset, the app uses http://127.0.0.1:8000.
# Start your SOL/FastAPI server on that host:port, or set this to your backend URL.
# VITE_API_URL=http://127.0.0.1:8000
